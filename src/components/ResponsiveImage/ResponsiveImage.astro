---
import './responsive-image.css';

interface Props {
  src: string;
  alt?: string;
  class?: string;
}

const { src, alt = '', class: className, ...rest } = Astro.props;
---
<img src={src} alt={alt} class:list={['fit-img', className]} {...rest} />
