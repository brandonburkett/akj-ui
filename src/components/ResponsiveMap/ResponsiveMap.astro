---
import './responsive-map.css';

interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={['fit-map', className]} {...rest}>
  <slot />
</div>
