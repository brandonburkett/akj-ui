---
import './below-fold.css';
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'div'>;

const { class: className, ...rest } = Astro.props;
---

<div class:list={['below-fold', 'group', className]} {...rest}>
  <slot />
</div>
