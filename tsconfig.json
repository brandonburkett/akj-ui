{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] }
  },
  "include": [".astro/types.d.ts", "**/*"],
  // aws is the AWS CLI that CircleCI unzips into the project during CI, not our source
  "exclude": ["dist", "coverage", "aws"]
}
